import 'package:flutter/material.dart';

class WorkoutSettingsView extends StatelessWidget {
  const WorkoutSettingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
